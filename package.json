{
  "name": "beauta-voice",
  "version": "1.0.0",
  "description": "Beauta's phone line: Twilio calls arrive here, an AI receptionist answers. Separate from beauta-api because a call is a long-lived socket and beauta-api deploys drop them.",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "npm run clean && tsc -p .",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit -p .",
    "prisma:sync": "node scripts/sync-schema.mjs && prisma generate",
    "prisma:generate": "prisma generate",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "migrate": "echo 'beauta-api owns this schema. Migrate there, then run npm run prisma:sync here.' && exit 1",
    "prisma:migrate": "echo 'beauta-api owns this schema. Migrate there, then run npm run prisma:sync here.' && exit 1",
    "db:push": "echo 'beauta-api owns this schema. Migrate there, then run npm run prisma:sync here.' && exit 1"
  },
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@fastify/cors": "11.3.0",
    "@fastify/formbody": "9.0.0",
    "@fastify/swagger": "9.6.1",
    "@fastify/swagger-ui": "5.2.3",
    "@fastify/websocket": "11.0.2",
    "@prisma/client": "6.19.3",
    "@sinclair/typebox": "0.34.41",
    "fastify": "5.12.1",
    "openai": "6.14.0",
    "twilio": "5.3.5"
  },
  "devDependencies": {
    "@types/node": "26.4.0",
    "@types/ws": "8.18.1",
    "prisma": "6.19.3",
    "tsx": "4.23.13",
    "typescript": "5.7.2"
  }
}
